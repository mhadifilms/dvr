---
name: Bug report
about: Report something `dvr` got wrong
labels: bug
---

## What happened

<!-- One sentence. -->

## Reproduction

```python
# minimal Python repro, or shell commands
```

## Expected vs actual

**Expected:**

**Actual:**

## Environment

- `dvr` version (`dvr --version`):
- DaVinci Resolve version:
- OS (macOS / Windows / Linux + version):
- Python version (`python --version`):

## Diagnostic output

If `dvr` raised a structured error, paste it verbatim — the `Cause:` / `Fix:` / `State:` lines are the most useful debugging signal.

```
```
